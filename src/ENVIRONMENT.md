DEFAULT_DEBUG   enable debug level for logging
DEFAULT_TRACING   enable tracing level for logging
DEFAULT_NAME   set the name of the instance of the service
DEFAULT_TITLE   set the title in the web page
DEFAULT_PORT_NB   the local port of the web service (default=8080)
DEFAULT_API_KEYS   space separated list of valid API keys
DEFAULT_SESSION_KEY
DEFAULT_POLICY   OPA policy for access control
DEFAULT_OPASVC   OPA service port to get the OPA policy for access control
DEFAULT_REALM   Basic authentication realm
DEFAULT_STAFF_USER   username of the administrator
DEFAULT_STAFF_PASSWORD   password of the administrator
DEFAULT_PARTICIPANT_USER   username of the user
DEFAULT_PARTICIPANT_PASSWORD   password of the user
DEFAULT_CERT_PEM   certificate for TLS (HTTPS) communication
DEFAULT_KEY_PEM   key for TLS (HTTPS) communication
DEFAULT_LOG_FILE   filename of the logging file or if it is "-" log all messages to the console
DEFAULT_LOKI_SERVER   URL of the Loki Server, e.g. the Grafana Cloud
DEFAULT_LOKI_USER   user name as defined for the data source as basic authentication
DEFAULT_LOKI_PASSWORD   password as defined for the data source as basic authentication
DEFAULT_LOKI_KEY   key/token as defined for the data source
DEFAULT_LABELS   set of labels for Grafana Loki, like "app:myapp,tenant:mycustomer"
DEFAULT_BUFFERSIZE   number of log lines cached before sending to Grafana Loki
DEFAULT_MAX_DELAY   max time in seconds after which logs are flushed from buffer
DEFAULT_LANGUAGE
DEFAULT_LANGUAGES
DEFAULT_USE_SSE   enable support for _server side event_ communication (default=false)
DEFAULT_PROGRESS_DURATION   default duration of the progress bar (default=100ms)
DEFAULT_RAPIDOC_DOC   enable Rapidoc for the OpenAPI viewer (default=false)
DEFAULT_ELEMENTS_DOC   enable Elements for the OpenAPI viewer (default=false)
